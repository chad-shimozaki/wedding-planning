<div class="home-header">
  May 15, 2026 – May 16, 2026
</div>

<div id="carousel_home" class="carousel slide" style="max-height: 80vh; overflow: hidden;width: 75%; margin: auto;" data-bs-ride="carousel">
  <div class="carousel-indicators">
    <button type="button" data-bs-target="#carousel_home" data-bs-slide-to="0" class="active" aria-current="true" aria-label="Slide 1"></button>
    <button type="button" data-bs-target="#carousel_home" data-bs-slide-to="1" aria-label="Slide 2"></button>
    <button type="button" data-bs-target="#carousel_home" data-bs-slide-to="2" aria-label="Slide 3"></button>
    <button type="button" data-bs-target="#carousel_home" data-bs-slide-to="3" aria-label="Slide 4"></button>
    <button type="button" data-bs-target="#carousel_home" data-bs-slide-to="4" aria-label="Slide 5"></button>
  </div>
  <div class="carousel-inner">
    <div class="carousel-item active" data-bs-interval="3000">
      <%= image_tag "artesa_both_5.JPG", alt: "The proposal with a beautiful view", class: "img-fluid", style:"object-fit: contain;" %>
    </div>
    <div class="carousel-item" data-bs-interval="3000">
      <%= image_tag "artesa_both_3.JPG", alt: "Closeup of wedding ring", class: "img-fluid", style:"object-fit: contain;" %>
    </div>
    <div class="carousel-item" data-bs-interval="3000">
      <%= image_tag "party_surprise.jpg", alt: "Surprise from family and friends", class: "img-fluid", style:"object-fit: contain;" %>
    </div>
    <div class="carousel-item" data-bs-interval="3000">
      <%= image_tag "party_family.jpg", alt: "Posed picture with family", class: "img-fluid", style:"object-fit: contain;" %>
    </div>
    <div class="carousel-item" data-bs-interval="3000">
      <%= image_tag "party_friends.jpg", alt: "Silly picture with friends", class: "img-fluid", style:"object-fit: contain;" %>
    </div>
  </div>
  <button class="carousel-control-prev" type="button" data-bs-target="#carousel_home" data-bs-slide="prev">
    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
    <span class="visually-hidden">Previous</span>
  </button>
  <button class="carousel-control-next" type="button" data-bs-target="#carousel_home" data-bs-slide="next">
    <span class="carousel-control-next-icon" aria-hidden="true"></span>
    <span class="visually-hidden">Next</span>
  </button>
</div>

<% if Event.exists? %>
  <div class="container my-5">
    <!-- Row 1: 3 Boxes -->
    <div class="row mb-4">
      <div class="col-md-4">
        <a href="/events/<%= Event.where({ :chronological_order => 1 }).first.id %>" class="text-decoration-none">
          <div class="card">
            <img src="<%= Event.where({ :chronological_order => 1 }).first.photo_url %>" class="card-img-top" alt="Image 1">
            <div class="card-body">
              <h5 class="card-title"><%= Event.where({ :chronological_order => 1 }).first.name %></h5>
              <p class="card-text"><%= Event.where({ :chronological_order => 1 }).first.date.strftime("%B %d, %Y") %></p>
            </div>
          </div>
        </a>
      </div>
      <div class="col-md-4">
        <a href="/events/<%= Event.where({ :chronological_order => 2 }).first.id %>" class="text-decoration-none">
          <div class="card">
            <img src="<%= Event.where({ :chronological_order => 2 }).first.photo_url %>" class="card-img-top" alt="Image 2">
            <div class="card-body">
              <h5 class="card-title"><%= Event.where({ :chronological_order => 2 }).first.name %></h5>
              <p class="card-text"><%= Event.where({ :chronological_order => 2 }).first.date.strftime("%B %d, %Y") %></p>
            </div>
          </div>
        </a>
      </div>
      <div class="col-md-4">
        <a href="/events/<%= Event.where({ :chronological_order => 3 }).first.id %>" class="text-decoration-none">
          <div class="card">
            <img src="<%= Event.where({ :chronological_order => 3 }).first.photo_url %>" class="card-img-top" alt="Image 3">
            <div class="card-body">
              <h5 class="card-title"><%= Event.where({ :chronological_order => 3 }).first.name %></h5>
              <p class="card-text"><%= Event.where({ :chronological_order => 3 }).first.date.strftime("%B %d, %Y") %></p>
            </div>
          </div>
        </a>
      </div>
    </div>

    <!-- Row 2: 2 Boxes -->
    <div class="row">
      <div class="col-md-6">
        <a href="/events/<%= Event.where({ :chronological_order => 4 }).first.id %>" class="text-decoration-none">
          <div class="card">
            <img src="<%= Event.where({ :chronological_order => 4 }).first.photo_url %>" class="card-img-top" alt="Image 4">
            <div class="card-body">
              <h5 class="card-title"><%= Event.where({ :chronological_order => 4 }).first.name %></h5>
              <p class="card-text"><%= Event.where({ :chronological_order => 4 }).first.date.strftime("%B %d, %Y") %></p>
            </div>
          </div>
        </a>
      </div>
      <div class="col-md-6">
        <a href="/events/<%= Event.where({ :chronological_order => 5 }).first.id %>" class="text-decoration-none">
          <div class="card">
            <img src="<%= Event.where({ :chronological_order => 5 }).first.photo_url %>" class="card-img-top" alt="Image 5">
            <div class="card-body">
              <h5 class="card-title"><%= Event.where({ :chronological_order => 5 }).first.name %></h5>
              <p class="card-text"><%= Event.where({ :chronological_order => 5 }).first.date.strftime("%B %d, %Y") %></p>
            </div>
          </div>
        </a>
      </div>
    </div>
  </div>
<% else %>
  <div style="margin: 20px">
    <h6>No events available at the moment. Check back later!</h6>
  </div>
<% end %>

<div style="display: flex; justify-content: center; align-items: center; margin-bottom: 20px;">
  <a href="/katsu" class="brown-button" type="button">
    Woof Woof! (<em>Click me!</em>)
  </a>
</div>
